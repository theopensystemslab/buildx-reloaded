export default function IndexPage() {
  // const { data: allHouseTypes } = useAllHouseTypes()
  // useInitSystemModules({ systemId: "skylark" })
  // const elements = useSystemElements({ systemId: "skylark" })

  // const materials = useSystemMaterials({ systemId: "skylark" })

  // if (!elements) {
  //   return <div>Loading...</div>
  // }

  return <div>{/* <pre>{JSON.stringify(elements, null, 2)}</pre> */}</div>
}
