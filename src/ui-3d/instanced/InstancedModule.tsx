import React, { Fragment } from "react"

type Props = {
  dna: string
  systemId: string
}

const InstancedModule = (props: Props) => {
  // each house has a layout, position, rotation

  // each module within that house also

  // must identify occurrences within layouts

  return <Fragment>{null}</Fragment>
}

export default InstancedModule
