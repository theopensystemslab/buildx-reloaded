import React, { Fragment } from "react"

type Props = {
  dna: string
  systemId: string
}

// this is all modules across all houses now
const InstancedSystemModules = (props: Props) => {
  // so query some data structure by dna and system id...

  // so that's the houses proxy

  // well, you need layouts derivations on house dna's

  return <Fragment>{null}</Fragment>
}

export default InstancedSystemModules
